# Projects

Active and archived projects. Each is a note with `type: project`, a **Timeline**,
and a **Decision log**. Start one by copying `templates/project.md` into
`projects/active/`, then link it under **Active** below. Move it to
`projects/archived/` and set `status: done` when finished.

## Active

_None yet. Copy `templates/project.md` into `projects/active/` and link it here._

## Archived

_Finished projects move here with `status: done`._
