@AGENTS.md

<!--
Claude Code reads CLAUDE.md, not AGENTS.md (no fallback). The line above imports the
shared, agent-agnostic instructions so there is ONE source of truth for every agent —
edit AGENTS.md to change them. Add any Claude-Code-specific notes (plan-mode rules,
skill pointers) BELOW this comment; they append after the shared instructions.
-->
